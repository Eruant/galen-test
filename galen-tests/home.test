@@ set
    domain      localhost
    base_url    https://${domain}/

@@ table devices
    | device    | size      |
    | mobile    | 500x700   |
    | tablet    | 900x600   |
    | desktop   | 1300x700  |

@@ parameterized using devices
Home on ${device} device
    ${websiteUrl} ${size}
        check galen-tests/specs/home.spec --include "${device}"
